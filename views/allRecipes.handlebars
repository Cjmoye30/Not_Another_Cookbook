<h1>All Our Recipes</h1>

{{#each recipeArr}}
<div class="recipeGroup">
    <h4>{{recipe_name}}</h4>
    <p>{{description}}</p>
    <p>{{date_created}}</p>
    {{!-- <p>{{images.[0].filePath}}</p> --}}
    <img class="image-upload" src="/uploads/{{images.[0].filePath}}" alt="delish">
</div>

<hr>

{{/each}}